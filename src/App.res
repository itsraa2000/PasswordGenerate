@react.component
let make = () => {

  // let onGenerate = _evt => {
  //   RescriptReactRouter.push("/");
  // };
  
  <div className="p-6">
    <h1 className="text-maindark text-4xl font-semibold"> {"Passsword Generator"->React.string} </h1>
    // <Button
    //   onClick={onGenerate}
    //   label="GENERATE"
    // />
  </div>
}
